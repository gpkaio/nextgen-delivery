# Service-layer dependencies only -- nextgen_engine.py and test_engine.py
# stay pure stdlib (CLAUDE.md house rule 2). Install into a venv:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
chromadb==1.5.9
fastapi==0.141.1
uvicorn==0.52.3
